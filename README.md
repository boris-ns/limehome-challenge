# limehome-challenge

## Project setup

```bash
$ cp .env.example .env
$ sudo docker-compose up -d
$ npm install
$ npm run post-setup
$ npm run start:dev
```

## Run unit tests

```bash
$ npx jest
```
